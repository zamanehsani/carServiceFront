import DealTable from "./dealtable"
// import { useDispatch, useSelector } from "react-redux";
// const auth = useSelector((state)=>state.auth)

export default function Home(){
    // get the userdetails on page load

    return(
        <>
            <DealTable/>
        </>
    )
}